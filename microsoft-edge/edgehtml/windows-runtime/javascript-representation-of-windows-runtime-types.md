---
description: JavaScript-Darstellung von Windows Runtime-Typen
title: JavaScript-Darstellung von Windows Runtime-Typen
ms.custom: ''
ms.date: 10/30/2020
ms.prod: microsoft-edge
ms.technology: windows-integration
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
ROBOTS: NOINDEX,NOFOLLOW
ms.openlocfilehash: 18a288f95e7211794e3ceb88d700026dae359b06
ms.sourcegitcommit: 6cf12643e9959873f8b5d785fd6158eeab74f424
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 03/06/2021
ms.locfileid: "11399330"
---
# <a name="javascript-representation-of-windows-runtime-types"></a><span data-ttu-id="22d43-103">JavaScript-Darstellung von Windows-Runtime-Typen</span><span class="sxs-lookup"><span data-stu-id="22d43-103">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="22d43-104">In der folgenden Tabelle wird beschrieben, wie JavaScript Windows-Runtime-Typen darstellt.</span><span class="sxs-lookup"><span data-stu-id="22d43-104">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="22d43-105">Windows-Runtime-Features sind für Apps, die in Internet Explorer ausgeführt werden, nicht verfügbar.</span><span class="sxs-lookup"><span data-stu-id="22d43-105">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <a name="windows-runtime-types-in-javascript"></a><span data-ttu-id="22d43-106">Windows-Runtime-Typen in JavaScript</span><span class="sxs-lookup"><span data-stu-id="22d43-106">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="22d43-107">Windows-Runtime-Typ</span><span class="sxs-lookup"><span data-stu-id="22d43-107">Windows Runtime type</span></span> | <span data-ttu-id="22d43-108">JavaScript-Darstellung</span><span class="sxs-lookup"><span data-stu-id="22d43-108">JavaScript representation</span></span> | <span data-ttu-id="22d43-109">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="22d43-109">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="22d43-110">Eine Windows-Runtime `Uint8` ist eine nicht signierte 8-Bit-Ganzzahl, die als JavaScript-Nummer dargestellt wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-110">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="22d43-111">Ein JavaScript-Wert wird zunächst in eine JavaScript-Nummer konvertiert, und dann wird `ToUint32` der Prozess befolgt.</span><span class="sxs-lookup"><span data-stu-id="22d43-111">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="22d43-112">Wenn sich der Wert der JavaScript-Nummer außerhalb des Bereichs eines Uint8 befindet, wird modulo 2^8 angewendet.</span><span class="sxs-lookup"><span data-stu-id="22d43-112">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="22d43-113">Eine Windows-Runtime `Int32` ist eine signierte 32-Bit-Ganzzahl, die als JavaScript-Zahl dargestellt wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-113">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="22d43-114">Ein JavaScript-Wert wird zunächst in eine JavaScript-Nummer konvertiert, und dann wird `ToInt32` der Prozess befolgt.</span><span class="sxs-lookup"><span data-stu-id="22d43-114">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="22d43-115">Wenn sich der Wert der JavaScript-Nummer außerhalb des Bereichs eines befindet, wird `Int32` modulo 2^16 angewendet.</span><span class="sxs-lookup"><span data-stu-id="22d43-115">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="22d43-116">Eine #A0 ist eine signierte ganzzahlige 64-Bit-Zahl, die als Standardnummer dargestellt wird, wenn sie in den Bereich `Int64` \[-2^53, 2^53\] fällt.</span><span class="sxs-lookup"><span data-stu-id="22d43-116">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="22d43-117">Wenn er außerhalb dieses Bereichs liegt, wird er als Zahlenwert dargestellt, der über einen benutzerdefinierten Sicherungsspeicher verfügt, der die vollständigen 64 Bit ganzzahliger Daten verwaltet.</span><span class="sxs-lookup"><span data-stu-id="22d43-117">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="22d43-118">Alle mathematischen Vorgänge für diese benutzerdefinierten Int64-Nummernwerte führen dazu, dass der Wert in eine Standardnummer im Bereich \[-2^53, 2^53\] konvertiert wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-118">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="22d43-119">Wenn sich der Wert außerhalb dieses Bereichs befindet, wird ein Typfehler ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="22d43-119">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="22d43-120">Die Ausnahmen für diesen Konvertierungsprozess sind die Vorgänge , , und, die die Argumente basierend auf den vollständigen 64 Bit vergleichen, wenn zwei dargestellte `==` `===` `SameValue` `RelationalComparison` 64-Bit-Werte übergeben werden.</span><span class="sxs-lookup"><span data-stu-id="22d43-120">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="22d43-121">Wenn es sich bei beiden Argumenten um eine Standardmäßige JavaScript-Nummer handelt, werden diese Vorgänge auch vor dem Vergleich in eine JavaScript-Zahl konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-121">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="22d43-122">Ein #A0 wird direkt zugewiesen, wenn es sich um einen Int64-Wert selbst handelt. Andernfalls wird das Ergebnis der Konvertierung `ToInteger` auf den Wert an die Windows-Runtime übergeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-122">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="22d43-123">Wenn bei der Typkoercion ein Fehler auftritt, wird eine Ausnahme zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-123">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="22d43-124">Eine #A0 ist eine nicht signierte ganzzahlige 64-Bit-Zahl, die als Standardnummer dargestellt wird, wenn sie in den Bereich `Uint64` \[0, 2^53\] fällt.</span><span class="sxs-lookup"><span data-stu-id="22d43-124">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="22d43-125">Wenn sie außerhalb dieses Bereichs liegt, wird sie als Zahl dargestellt, die über einen benutzerdefinierten Sicherungsspeicher verfügt, der die vollständigen 64 Bit nicht signierter ganzzahliger Daten verwaltet.</span><span class="sxs-lookup"><span data-stu-id="22d43-125">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="22d43-126">Alle mathematischen Vorgänge für diese benutzerdefinierten Uint64-Werte führen dazu, dass der Wert in eine Standardnummer im Bereich \[0, 2^53\] konvertiert wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-126">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="22d43-127">Wenn sich der Wert außerhalb dieses Bereichs befindet, wird ein Typfehler ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="22d43-127">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="22d43-128">Die Ausnahmen bei diesem Konvertierungsprozess sind die Vorgänge , , und, die die Argumente basierend auf den vollständigen 64 Bit vergleichen, wenn zwei `==` `===` `SameValue` `RelationalComparison` 64-Bit-Werte übergeben werden.</span><span class="sxs-lookup"><span data-stu-id="22d43-128">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="22d43-129">Wenn es sich bei beiden Argumenten um eine Standardmäßige JavaScript-Nummer handelt, werden diese Vorgänge auch vor dem Vergleich in eine JavaScript-Zahl konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-129">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="22d43-130">Ein #A0 wird direkt zugewiesen, wenn es sich um einen Uint64-Wert selbst handelt. Andernfalls wird das Ergebnis der Anwendung der Konvertierung auf den Wert, gefolgt von `ToInteger` modulo 2^52, an die Windows-Runtime übergeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-130">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="22d43-131">Wenn bei der Typkonvertierung ein Fehler auftritt, wird eine Ausnahme zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-131">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="22d43-132">Bei einer Windows-Runtime handelt es sich um eine 32-Bit-Gleitkommazahl, die als JavaScript-Zahl dargestellt wird, indem der nächste Wert mit doppelter Genauigkeit zum Wert mit einer `Single` einzelnen Genauigkeit verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-132">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="22d43-133">Ein JavaScript-Wert wird in eine JavaScript-Zahl konvertiert und dann der Bereich überprüft, um sicherzustellen, dass der Wert in einer 32-Bit-IEEE 754-Gleitkommazahl mit einer Genauigkeit dargestellt werden kann.</span><span class="sxs-lookup"><span data-stu-id="22d43-133">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="22d43-134">Wenn die Konvertierungs- oder Bereichsüberprüfung fehlschlägt, wird ein Marshallingfehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-134">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="22d43-135">Eine Windows-Runtime `Double` ist eine 64-Bit-Gleitkommazahl.</span><span class="sxs-lookup"><span data-stu-id="22d43-135">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="22d43-136">wird verwendet, um eine Windows-Runtime `Double` in eine JavaScript-Nummer zu konvertieren.</span><span class="sxs-lookup"><span data-stu-id="22d43-136">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="22d43-137">Wenn die Konvertierung fehlschlägt, wird ein Marshallingfehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-137">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="22d43-138">Eine Windows-Runtime ist ein 8-Bit-Wert, bei dem Null und jeder andere Wert als `Boolean` Null ist , dargestellt als `false` `true` JavaScript `Boolean` .</span><span class="sxs-lookup"><span data-stu-id="22d43-138">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="22d43-139">Ein JavaScript-Wert wird zuerst von in ein JavaScript `Boolean` `ToBoolean` konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-139">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="22d43-140">Dies bedeutet, dass eine Windows-Runtime-Funktion, die als deklariert `void func(BOOL);` wurde, in JavaScript als geschrieben werden `obj.func("test");` könnte.</span><span class="sxs-lookup"><span data-stu-id="22d43-140">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="22d43-141">Die Windows-Runtime-Funktion wird aufgerufen, wenn der `BOOL` Parameter als übergeben `TRUE` wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-141">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="22d43-142">Wenn die Konvertierung fehlschlägt, wird ein Marshallingfehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-142">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="22d43-143">Eine Windows-Runtime `Char16` ist ein 16-Bit-Unicode-Zeichen, das als JavaScript-Zeichenfolge dargestellt wird, die ein einzelnes Zeichen enthält.</span><span class="sxs-lookup"><span data-stu-id="22d43-143">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="22d43-144">Ein JavaScript-Wert wird von in eine JavaScript-Zeichenfolge konvertiert und dann überprüft, um sicherzustellen, dass die Zeichenfolge nur ein einzelnes `ToString` Zeichen lang ist.</span><span class="sxs-lookup"><span data-stu-id="22d43-144">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="22d43-145">Das einzelne Zeichen wird dann als Wert `Char16` übergeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-145">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="22d43-146">Wenn die Konvertierungs- oder Längenüberprüfung fehlschlägt, wird ein Marshallingfehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-146">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="22d43-147">Eine Windows-Runtime `String` ist eine unveränderliche Abfolge von `Char16` Objekten.</span><span class="sxs-lookup"><span data-stu-id="22d43-147">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="22d43-148">Zeichenfolgen werden als JavaScript-Objekte `String` dargestellt.</span><span class="sxs-lookup"><span data-stu-id="22d43-148">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="22d43-149">Windows-Runtime-Zeichenfolgen haben keine unterschiedlichen Werte für und `null` leere Zeichenfolge \(""\).</span><span class="sxs-lookup"><span data-stu-id="22d43-149">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="22d43-150">und leere Zeichenfolgenwerte werden in die leere JavaScript-Zeichenfolge konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-150">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="22d43-151">**HINWEIS**: Wenn JavaScript an einen Windows-Runtime-Parameter übergeben wird, der eine Zeichenfolge erwartet, wird der Zeichenfolgenwert "null" oder die leere Zeichenfolge `null` `null` \(""\) erzeugt.</span><span class="sxs-lookup"><span data-stu-id="22d43-151">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="22d43-152">Zeichenfolgen, die an die Windows-Runtime übergeben werden, sollten immer an die leere Zeichenfolge instanziiert werden.</span><span class="sxs-lookup"><span data-stu-id="22d43-152">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="22d43-153">Eine Windows-Runtime ist eine 32-Bit-Ganzzahl \(signiert oder nicht signiert\), der eine Reihe benannter `Enumeration` Konstanten zugeordnet ist.</span><span class="sxs-lookup"><span data-stu-id="22d43-153">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="22d43-154">In JavaScript wird eine Enumeration als Objekt dargestellt, das ein schreibgeschütztes Feld für jeden benannten Wert enthält.</span><span class="sxs-lookup"><span data-stu-id="22d43-154">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="22d43-155">Enumerationswerte werden in JavaScript-Zahlen konvertiert, wie zuvor für und `Int32` `UInt32` definiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-155">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="22d43-156">Wenn ein JavaScript-Wert in eine Windows-Runtime-Enumeration konvertiert wird, wird er wie zuvor beschrieben konvertiert, abgeschnitten und der Bereich überprüft.</span><span class="sxs-lookup"><span data-stu-id="22d43-156">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="22d43-157">Der resultierende Wert wird jedoch nicht mit den definierten benannten Werten überprüft, die in der Enumeration angegeben sind.</span><span class="sxs-lookup"><span data-stu-id="22d43-157">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="22d43-158">Eine Windows-Runtime `Structure` ist eine heterogene Auflistung benannter Datenfelder.</span><span class="sxs-lookup"><span data-stu-id="22d43-158">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="22d43-159">In JavaScript wird eine Struktur als JavaScript-Objekt dargestellt, das eine benannte Eigenschaft für jedes Feld in der Struktur enthält.</span><span class="sxs-lookup"><span data-stu-id="22d43-159">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="22d43-160">Wenn bei der Konvertierung eines Strukturwerts ein Fehler auftritt, wird ein Marshallingfehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-160">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="22d43-161">Felder im JavaScript-Objekt, die keine Entsprechungen in der Windows-Runtime-Struktur haben, werden ignoriert.</span><span class="sxs-lookup"><span data-stu-id="22d43-161">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="22d43-162">**HINWEIS:** Eine Windows-Runtime-Struktur kann nicht in JavaScript mit dem Schlüsselwort `new` instanziiert werden.</span><span class="sxs-lookup"><span data-stu-id="22d43-162">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="22d43-163">Eine Windows-Runtime `Array` wird in ein JavaScript-Array konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-163">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="22d43-164">Windows-Runtime-Arrays können jedoch nicht in der Größe gespeichert werden, sodass einige JavaScript-Arrayvorgänge nicht möglich sind.</span><span class="sxs-lookup"><span data-stu-id="22d43-164">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="22d43-165">Die interne Eigenschaft eines konvertierten Arrays ist nicht auf "Array" festgelegt, da dies in der `[[Class]]` ECMAScript 5-Spezifikation nicht zulässig ist.</span><span class="sxs-lookup"><span data-stu-id="22d43-165">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="22d43-166">Dies bedeutet, dass `Array.isArray(v)` zurückgegeben `false` wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-166">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="22d43-167">Ein JavaScript-Wert wird wie folgt in ein Windows-Runtime-Array konvertiert: Wenn der Wert oder ist, wird `null` er in ein `undefined` systemeigenes `null` konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-167">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="22d43-168">Wenn die interne Eigenschaft "Array" ist, wird sie in ein systemeigenes Array kopiert, und ein `[[Class]]` Verweis auf dieses Array wird übergeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-168">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="22d43-169">Wenn es sich wie zuvor beschrieben um ein konvertiertes Array handelt, wird das zugrunde liegende systemeigene Array übergeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-169">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="22d43-170">**HINWEIS**: Das Übergeben eines JavaScript-Arraywerts an eine Windows-Runtime-Methode führt zu einer vollständigen Kopie des Arrays.</span><span class="sxs-lookup"><span data-stu-id="22d43-170">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="22d43-171">Delegate \(function callback\)</span><span class="sxs-lookup"><span data-stu-id="22d43-171">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="22d43-172">Ein Windows-Runtime-Delegater ist ein Verweis auf eine einzelne Methode.</span><span class="sxs-lookup"><span data-stu-id="22d43-172">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="22d43-173">Ein Windows-Runtime-Delegate wird in JavaScript als JavaScript dargestellt, das garantiert im `Function` richtigen Thread aufgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="22d43-173">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="22d43-174">Wenn der `Function` aufgerufen wird, werden die Argumente in die entsprechenden Windows-Runtime-Parametertypen konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-174">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="22d43-175">Wenn weniger JavaScript-Argumente als Stellvertretungsparameter angegeben `in` sind, schlägt der Delegataufruf fehl.</span><span class="sxs-lookup"><span data-stu-id="22d43-175">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="22d43-176">Wenn mehr JavaScript-Argumente als Parameter im Delegaten enthalten sind, werden die `in` zusätzlichen JavaScript-Argumente ignoriert.</span><span class="sxs-lookup"><span data-stu-id="22d43-176">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="22d43-177">Nachdem der Delegat aufgerufen wurde, werden `out` die Parameter in JavaScript-Typen konvertiert und zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-177">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="22d43-178">Wenn ein systemeigenes JavaScript-Funktionsobjekt in einen Windows-Runtime-Delegaten konvertiert wird, wird es in einen Windows-Runtime-Delegaten des entsprechenden Typs umschlossen.</span><span class="sxs-lookup"><span data-stu-id="22d43-178">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="22d43-179">Wenn der Delegat aufgerufen wird, werden `in` die Parameter in JavaScript-Typen konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-179">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="22d43-180">Nachdem der Delegat aufgerufen wurde, wird der Rückgabewert in die `out` Parameter des Delegaten konvertiert.</span><span class="sxs-lookup"><span data-stu-id="22d43-180">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="22d43-181">Schnittstelle</span><span class="sxs-lookup"><span data-stu-id="22d43-181">Interface</span></span> | `Object` | <span data-ttu-id="22d43-182">Schnittstellen und Schnittstellengruppen werden in JavaScript nicht direkt als Objekte dargestellt.</span><span class="sxs-lookup"><span data-stu-id="22d43-182">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="22d43-183">Schnittstellen werden jedoch als Parameter- und Rückgabetypen von Windows-Runtime-Methoden dargestellt.</span><span class="sxs-lookup"><span data-stu-id="22d43-183">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="22d43-184">Eine Windows-Runtime-Schnittstelleninstanz wird wie folgt konvertiert:</span><span class="sxs-lookup"><span data-stu-id="22d43-184">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="22d43-185">1. Wenn eine gültige Laufzeitklasse vor ist, stellen Sie das Objekt als Instanz dieser Klasse dar.</span><span class="sxs-lookup"><span data-stu-id="22d43-185">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="22d43-186">2. Wenn keine gültige Laufzeitklasse verfügbar ist, stellen Sie das Objekt als Instanz einer unbenannten Laufzeitklasse dar, die genau die Schnittstelle implementiert, die die Instanz sowie die erforderlichen Schnittstellen implementieren kann.</span><span class="sxs-lookup"><span data-stu-id="22d43-186">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="22d43-187">Ein JavaScript-Wert wird getestet, um zu ermitteln, ob es sich um eine Instanz einer Laufzeitklasse oder einer Schnittstelle handelt.</span><span class="sxs-lookup"><span data-stu-id="22d43-187">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="22d43-188">Wenn dies der Zustand ist und der ursprüngliche Windows-Runtime-Wert die Schnittstelle implementiert, wird das Objekt an die Windows-Runtime übergeben.</span><span class="sxs-lookup"><span data-stu-id="22d43-188">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="22d43-189">Laufzeitklassen</span><span class="sxs-lookup"><span data-stu-id="22d43-189">Runtime classes</span></span> | `Object` | <span data-ttu-id="22d43-190">Objekte in der Windows-Runtime können Instanzen von Laufzeitklassen sein, die eine oder mehrere Schnittstellen implementieren.</span><span class="sxs-lookup"><span data-stu-id="22d43-190">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="22d43-191">Windows-Runtime-Objekte werden in JavaScript als Objekte dargestellt.</span><span class="sxs-lookup"><span data-stu-id="22d43-191">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="22d43-192">Die Vereinigung von Methoden, Eigenschaften und Ereignissen, die für alle implementierten Schnittstellen der Klasse definiert sind, stellt die benannten Eigenschaften im Prototyp des JavaScript-Objekts dar.</span><span class="sxs-lookup"><span data-stu-id="22d43-192">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="22d43-193">JavaScript-Benutzer können direkt auf jedes Element der Klasse zugreifen.</span><span class="sxs-lookup"><span data-stu-id="22d43-193">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="22d43-194">Windows-Runtime-Objekte verfügen über einen Prototyp, der alle Member aller implementierten Schnittstellen der Laufzeitklasse enthält.</span><span class="sxs-lookup"><span data-stu-id="22d43-194">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <a name="see-also"></a><span data-ttu-id="22d43-195">Weitere Informationen</span><span class="sxs-lookup"><span data-stu-id="22d43-195">See also</span></span>  

[<span data-ttu-id="22d43-196">Verwenden der Windows-Runtime in JavaScript</span><span class="sxs-lookup"><span data-stu-id="22d43-196">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "Verwenden der Windows-Runtime in JavaScript-| Microsoft Docs"  
